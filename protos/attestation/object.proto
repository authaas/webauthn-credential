syntax = "proto3";

package attestation;

// Object is the attestation object of a registration, as the authenticator
// returned it: the exact bytes, never re-encoded. It carries the credential's
// public key, initial sign count, and flags.
message Object {
  bytes bytes = 1;
}
