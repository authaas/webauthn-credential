syntax = "proto3";

package webauthn;

// Credential is a public key an authenticator created for a principal, named
// by the id the authenticator chose.
message Credential {
  // Between 1 and 1023 bytes. The type is always "public-key" and is not
  // carried.
  bytes id = 1;
}
