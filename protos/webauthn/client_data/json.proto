syntax = "proto3";

package webauthn.client_data;

// JSON is the client data of a ceremony, as the authenticator signed it: the
// exact bytes, never parsed and re-serialized.
message JSON {
  bytes bytes = 1;
}
