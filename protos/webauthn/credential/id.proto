syntax = "proto3";

package webauthn.credential;

// ID is the identifier of a credential an authenticator created for an identity.
message ID {
  // Between 1 and 1023 bytes. The type is always "public-key" and is not
  // carried.
  bytes bytes = 1;
}
