syntax = "proto3";

package authenticator;

// Data is the authenticator data of an assertion, as the authenticator
// produced it: the exact bytes the signature covers together with the client
// data hash.
message Data {
  bytes bytes = 1;
}
